const mail = require('./mail/')
const arr = require('./arr/')
const str = require('./str/')
const datetime = require('./datetime/')
const redis = require('./redis/')

module.exports = {mail,arr,str,datetime,redis}